#pragma once

class CVirtualKey
{
public:
	CVirtualKey(void);
	virtual ~CVirtualKey(void);

	static WORD parse(wchar_t *s);
	static const wchar_t* toWString(WORD k);
	static const char* toString(WORD k);
};
